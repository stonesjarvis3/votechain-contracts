export default function ProposalDetail() {
  return (
    <section aria-labelledby="proposal-detail-heading" style={{ padding: '1.5rem' }}>
      <h2 id="proposal-detail-heading">Proposal Detail</h2>
      <p>Select a proposal to view its details.</p>
    </section>
  );
}
