# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: VoteChain Dashboards
    orgId: 1
    folder: VoteChain
    type: file
    disableDeletion: false
    options:
      path: /etc/grafana/provisioning/dashboards
