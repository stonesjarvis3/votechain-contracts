[package]
name = "votechain-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.8.1", features = ["macros"] }
tower-governor = { version = "0.4.2", features = ["axum"] }
tokio = { version = "1.44.2", features = ["rt-multi-thread", "macros"] }
